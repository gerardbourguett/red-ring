---
import { Button } from "@nextui-org/react";
import Countdown from "../components/react/Countdown";
import Layout from "../layouts/Layout.astro";
import { CURRENT_YEAR } from "../data/constants";
---

<Layout title=`#${CURRENT_YEAR + 1}Live | Bye Bye 2024`>
  <main class="min-h-screen flex items-center justify-center">
    <div class="text-center space-y-12 px-4 animate-fade-in">
      <h1 class="text-6xl md:text-8xl font-medium tracking-tighter">
        #Chao<span class="text-red-500">{CURRENT_YEAR}</span>
      </h1>

      <div class="max-w-4xl mx-auto">
        <Countdown client:load />
      </div>

      <Button
        color="default"
        variant="shadow"
        size="lg"
        className="text-xl hover:scale-105 transition-transform"
      >
        <a href="/">#{CURRENT_YEAR + 1}Live</a>
      </Button>
    </div>
  </main>
</Layout>

<style>
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fadeIn 0.6s ease-out;
  }
</style>
